// Sub-account view of the unified Templates page. The page reads the
// current scope from useAccount() and prefixes navigation via
// useSubaccountHref(), so the admin page serves both contexts.
export { default } from '@/app/templates/page';
