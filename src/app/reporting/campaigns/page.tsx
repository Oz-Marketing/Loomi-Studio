import { ReportingPageHeader } from '../_components/page-header';

export default function ReportingCampaignsPage() {
  return (
    <ReportingPageHeader
      eyebrow="Campaigns"
      title="Campaign reporting"
      subtitle="Per-campaign sends, opens, clicks, and conversions land here."
    />
  );
}
